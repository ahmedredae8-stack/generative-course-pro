name: Build Android (unsigned APK + AAB)

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: bun install

      - name: Install Capacitor
        run: bun add @capacitor/core @capacitor/cli @capacitor/android

      - name: Build web assets
        run: bun run build

      - name: Ensure webDir exists
        run: mkdir -p dist && [ -f dist/index.html ] || echo '<!doctype html><meta http-equiv="refresh" content="0;url=https://generative-course-pro.lovable.app">' > dist/index.html

      - name: Add Android platform
        run: bunx cap add android

      - name: Sync Capacitor
        run: bunx cap sync android

      - name: Force update package name in android project
        run: |
          sed -i 's/applicationId ".*"/applicationId "app.lovable.nilex"/g' android/app/build.gradle

      - name: Update to modern Google Play target and dependencies
        run: |
          # تحديث إعدادات الـ SDK لضمان التوافق مع أحدث متطلبات متجر بلاي (Android 15 / API 35 وما فوق)
          sed -i 's/compileSdkVersion = .*/compileSdkVersion = 35/g' android/build.gradle || true
          sed -i 's/targetSdkVersion = .*/targetSdkVersion = 35/g' android/build.gradle || true
          sed -i 's/compileSdk .*/compileSdk 35/g' android/app/build.gradle || true
          sed -i 's/targetSdk .*/targetSdk 35/g' android/app/build.gradle || true

      - name: Build unsigned APK
        working-directory: android
        run: ./gradlew assembleRelease

      - name: Build unsigned AAB
        working-directory: android
        run: ./gradlew bundleRelease

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nilex-android-unsigned
          path: |
            android/app/build/outputs/apk/release/*.apk
            android/app/build/outputs/bundle/release/*.aab
          if-no-files-found: error
